name = "tiktok-shop-checkout"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV Namespace binding
kv_namespaces = [
  { binding = "TIKTOK_ORDERS", id = "preview_id", preview_id = "preview_id" }
]

# Variables (você precisa adicionar ASAAS_API_KEY manualmente no dashboard)
[vars]
# ASAAS_API_KEY será configurada via dashboard do Cloudflare Workers
